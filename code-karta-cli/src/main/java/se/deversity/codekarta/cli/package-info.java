/**
 * CLI wiring for the parse → layout → render pipeline.
 * Null-marked: references are non-null by default.
 */
@NullMarked
package se.deversity.codekarta.cli;

import org.jspecify.annotations.NullMarked;
