<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>se.deversity.codekarta</groupId>
        <artifactId>code-karta</artifactId>
        <version>0.3.0</version>
    </parent>

    <artifactId>code-karta-maven-plugin</artifactId>
    <packaging>maven-plugin</packaging>

    <!-- For maven-plugin packaging this is not the deprecated prerequisites: maven-plugin-plugin
         writes it into the descriptor as requiredMavenVersion, so an older Maven refuses the
         plugin with a message naming the version instead of failing on a missing API. -->
    <prerequisites>
        <maven>${maven.api.version}</maven>
    </prerequisites>
    <name>CodeKarta Maven Plugin</name>
    <description>Generates code-karta diagrams as part of a Maven build</description>

    <properties>
        <!-- Runs inside the user's Maven JVM, so it targets the same floor as everything else.
             See <library.release> in the parent pom. -->
        <maven.compiler.release>${library.release}</maven.compiler.release>
        <maven.compiler.testRelease>21</maven.compiler.testRelease>

        <!-- Floors, not targets. Raise these when the numbers rise; never lower one to make a red
             build green. Measured over the hand-written classes only: the generated HelpMojo is
             excluded below, and left in it would put the bundle at 0.35 while saying nothing
             about code anyone here wrote. Same for the mutation score, which the mutation
             profile below excludes it from. Measured 0.89 line and 61% mutation. -->
        <jacoco.line.minimum>0.85</jacoco.line.minimum>
        <pitest.mutation.minimum>55</pitest.mutation.minimum>
    </properties>

    <dependencies>
        <dependency>
            <groupId>se.deversity.codekarta</groupId>
            <artifactId>code-karta-cli</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- provided: supplied by the Maven that runs the plugin, never bundled -->
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-plugin-api</artifactId>
            <version>${maven.api.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-core</artifactId>
            <version>${maven.api.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.plugin-tools</groupId>
            <artifactId>maven-plugin-annotations</artifactId>
            <version>${maven.plugin.tools.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
        </dependency>
        <dependency>
            <groupId>se.deversity.vibetags</groupId>
            <artifactId>vibetags-processor</artifactId>
            <version>${vibetags.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.jspecify</groupId>
            <artifactId>jspecify</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- HelpMojo is generated by the helpmojo goal below, into target/generated-sources.
                 Nothing here can test it and the next build regenerates it, so counting its 156
                 lines against this module measures the generator, not the module. -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>**/HelpMojo.class</exclude>
                    </excludes>
                </configuration>
            </plugin>
            <!-- The unit tests construct the mojo and call execute() directly, so they never see
                 Maven's own configurator binding a <configuration> block onto its fields. These
                 run the goal from a real Maven build against real poms: the shapes documented in
                 docs/MAVEN-PLUGIN.md, verified to do what that page says they do.

                 invoker:install stages this plugin and its dependencies into target/local-repo so
                 the IT builds resolve the module under test rather than a previously released
                 version. See the reactor trap in the repository conventions: a bare goal resolves
                 siblings from the repository. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-invoker-plugin</artifactId>
                <version>${maven.invoker.plugin.version}</version>
                <configuration>
                    <projectsDirectory>src/it</projectsDirectory>
                    <cloneProjectsTo>${project.build.directory}/it</cloneProjectsTo>
                    <localRepositoryPath>${project.build.directory}/local-repo</localRepositoryPath>
                    <settingsFile>src/it/settings.xml</settingsFile>
                    <postBuildHookScript>verify</postBuildHookScript>
                    <showErrors>true</showErrors>
                    <streamLogs>false</streamLogs>
                    <writeJunitReport>true</writeJunitReport>
                </configuration>
                <executions>
                    <execution>
                        <id>integration-test</id>
                        <goals>
                            <goal>install</goal>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <!-- The verify scripts are Groovy; the invoker plugin does not ship a runtime. -->
                    <dependency>
                        <groupId>org.apache.groovy</groupId>
                        <artifactId>groovy</artifactId>
                        <version>${groovy.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-plugin-plugin</artifactId>
                <version>${maven.plugin.tools.version}</version>
                <configuration>
                    <goalPrefix>karta</goalPrefix>
                </configuration>
                <executions>
                    <execution>
                        <id>default-descriptor</id>
                        <phase>process-classes</phase>
                        <goals><goal>descriptor</goal></goals>
                    </execution>
                    <!-- Generates the "how do I invoke this" help goal. Without it the plugin has
                         no self-documentation and users have to read the source or the site. -->
                    <execution>
                        <id>help-goal</id>
                        <goals><goal>helpmojo</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Mutation testing targets se.deversity.codekarta.* from the parent pom, which sweeps in
             the generated HelpMojo. Its mutants are unkillable here by construction, and left in
             they drag the score to 20% while saying nothing about hand-written code. -->
        <profile>
            <id>mutation</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-maven</artifactId>
                        <configuration>
                            <excludedClasses>
                                <param>se.deversity.codekarta.*.HelpMojo</param>
                            </excludedClasses>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
