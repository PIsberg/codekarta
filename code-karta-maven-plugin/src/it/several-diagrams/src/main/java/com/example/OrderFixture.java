package com.example;

// Named to match the goal-level <excludes> pattern, so the IT can prove the list actually
// reached the parser rather than merely being accepted by the configurator.
public class OrderFixture extends Order {
}
